
import CodeBlock from '@theme/CodeBlock';

import siteConfig from '@generated/docusaurus.config';

import AutomatedInstall from './_common/_automated-install.mdx';
import AutomatedUninstall from './_common/_automated-uninstall.mdx';
import ManualInstall from './_common/_manual-install.mdx';
import ManualInstallMacOS from './_common/_manual-install-macos.mdx';
import ManualInstallInfo from './_common/_manual-info.mdx';

{/* ------------------------------------------------------------------------ */}

<AutomatedInstall label={ props.label }/>

<CodeBlock language="console"> {
`$ tree -L 2 ~/Library/xPacks/\@xpack-dev-tools/openocd/${ siteConfig.customFields.upstreamVersion }-${ siteConfig.customFields.xpackSubversion }.${ siteConfig.customFields.npmSubversion }/.content/
/Users/ilg/Library/xPacks/\@xpack-dev-tools/openocd/${ siteConfig.customFields.upstreamVersion }-${ siteConfig.customFields.xpackSubversion }.${ siteConfig.customFields.npmSubversion }/.content/
├── README.md
├── bin
│   └── openocd
├── distro-info
│   └── licenses
├── libexec
│   ├── libftdi1.2.5.0.dylib
│   ├── libftdi1.2.dylib -> libftdi1.2.5.0.dylib
│   ├── libhidapi.0.14.0.dylib
│   └── libusb-1.0.0.dylib
└── openocd
    ├── OpenULINK
    ├── angie
    ├── contrib
    └── scripts

10 directories, 6 files`
}</CodeBlock>

<AutomatedUninstall/>

### Quick test

To check if the { siteConfig.customFields.appName } installed by **xpm** starts properly, use something like:

<CodeBlock language="console"> {
`$ ~/.local/xPacks/@xpack-dev-tools/openocd/${ siteConfig.customFields.upstreamVersion }-${ siteConfig.customFields.xpackSubversion }.${ siteConfig.customFields.npmSubversion }/.content/bin/openocd --version
${ siteConfig.customFields.upstreamVersion }`
}</CodeBlock>

<ManualInstall/>

<ManualInstallMacOS/>

<ManualInstallInfo/>

The result is a structure identical to the above.

### Quick test

To check if the { siteConfig.customFields.appName } installed manually starts properly, use something like:

<CodeBlock language="console"> {
`$ ~/.local/xPacks/openocd/${ siteConfig.customFields.upstreamVersion }-${ siteConfig.customFields.xpackSubversion }/bin/openocd --version
${ siteConfig.customFields.upstreamVersion }`
}</CodeBlock>

## Drivers

On macOS, JTAG probes implemented as USB devices usually are
recognised directly and do not need explicit drivers.
